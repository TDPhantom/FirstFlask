<!DOCTYPE html>
<html>

<div class="rpgui-content" style="overflow-y:scroll;">
    <div id="container">
	  <div id="main"></div>
      <div class="inner rpgui-container framed" style="position:relative">

      
    <head>
    <link rel="stylesheet" type="text/css" href="http://ronenness.github.io/RPGUI/rpgui/rpgui.min.css">
    <title> My First App</title>
    
 <div class="rpgui-progress">
<div class="rpgui-content">
</div>
    </head>




</div>


  <style>
	body, #container
	{
		background: #222;
		padding-top:20px;
		padding-bottom:20px;
	}
	
	.inner
	{
		background: #444;
		padding-left:20px;
		padding-right:20px;
		min-width: 740px;
		max-width: 900px;
		margin: 0 auto;

        
	}
  </style>
	<div id="progress" class="rpgui-container framed-grey">
				<h1>Progress Bars</h1>
				<p>This example shows the RPGUI progress bar class.</p>
				<hr />
				
				<div style="width: 40%; margin-left: 30%">
				
					<label>Health:</label>
					<div id="hp-bar" data-value="0.4" class="rpgui-progress red"></div>
					<label>Mana:</label>
					<div id="mana-bar" data-value="0.8" class="rpgui-progress blue"></div>
					<label>Stamina:</label>
					<div id="stamina-bar" data-value="0.2" class="rpgui-progress green"></div>
					<label>Experience:</label>
					<div id="stamina-bar" data-value="0.5" class="rpgui-progress"></div>
				</div>
    <body>



    
    <h1>Todo List</h1>
    <p>Welcome to my first frask website!</p>
    <p>this variable is from python: {{my_variable}}</p>
    <p>my goals: {{my_list}}</p>
    <ul>
    
     {% for things in todo_list%}

        <li style="{% if things['Done'] == 1 %}; color:red;{%endif%}"; color:black; font-size:20px; font-style:oblique;">{{things['description']}}
            {%if things['Done'] == 0 %}
        <form action="/Done" method="post">
            <input name="todo_list" value="{{things['ID']}}" hidden>
            <input type="submit" value="Done">
        </form>
        {%endif%}

        {{things['Description']}}
         </li>
     {% endfor %}
    </ul>
    <form
        action="/add"
        method="post"
    
    >

        <textarea name="new_todo">
        </textarea>
        <input type="submit" style="border-radius:12px
        padding:25px;
        ;background-color:black
        ;color:blue; 
        display:inline-block">
    
    
    
    </form>
</div>
    </body>

</html>